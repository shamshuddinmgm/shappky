# Feature backlog (Shappky Async)

Phase 0–1 (fork, identity, build, ADB smoke) are done.

**Next:** list the features you want here (or in chat). We implement one at a time → `assembleArm64-v8aDebug` → `adb install -r` → you try on device → push when satisfied.

## Suggested starters (from upstream issues — pick or ignore)

- Import / export settings & app filter lists
- Long-press running app → add to filter
- Home-screen kill shortcut / widget polish
- UI layout fixes (RAM row truncation on some Pixels)

## Your list

_(awaiting Mohammed)_
